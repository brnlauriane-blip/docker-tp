export interface Transaction {
  id: string;
  date: string;
  amount: number;
  balance: number;
  label: string;
  description: string;
}

export interface TransactionDetail {
  id: string;
  date: string;
  amount: number;
  balance: number;
  label: string;
  description: string;
}
